# detailing-calculator

Detailing calculator for Lisenok.

## Deploy (GitHub Pages)

1. Make sure GitHub Pages is configured to deploy from branch `main` and folder `/ (root)`.
2. Deploy command:

```bash
git push origin main
```
